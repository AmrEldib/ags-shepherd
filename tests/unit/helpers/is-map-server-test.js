
import { isMapServer } from 'ags-shepherd/helpers/is-map-server';
import { module, test } from 'qunit';

module('Unit | Helper | is map server');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isMapServer([42]);
  assert.ok(result);
});

